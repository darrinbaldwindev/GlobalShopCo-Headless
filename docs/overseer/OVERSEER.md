# GlobalShopCo-Headless Overseer Log

> **Record type:** append-only project evidence and task log. This log is not application source, a deployment record, or implementation authority.

## Project bootstrap — pending documentation authorization

**Decision basis:** Darrin selected the private `GlobalShopCo-Headless` repository as the future shared WordPress headless source for Global Shop Co.

**Verified baseline:** The repository was created private with default branch `main` and an initial `README.md` only. It contains no application, configuration, architecture, workflow, integration, or deployment content at bootstrap.

**Canonical relationship:** `GlobalShopCo` remains the parent architecture record. Shopify remains the commerce and checkout authority. This repository has no implementation-ready or production-ready claim.

**Standing boundary:** No code, dependency, credential, provider, hosting, Shopify access, product data, checkout action, test run, merge, deployment, or release occurs unless separately owner-authorized.

**Initial successor:** Reassess only after a separately authorized documentation change or a named implementation/hosting decision. Preserve exact revision, evidence class, owner decision, permitted/prohibited action, and verification criteria in each later append-only entry.
